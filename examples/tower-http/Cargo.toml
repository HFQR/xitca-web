[package]
name = "xitca-web-tower"
version = "0.1.0"
authors = ["fakeshadow <24548779@qq.com>"]
edition = "2024"

[dependencies]
xitca-web = { workspace = true, features = ["tower-http-compat"] }

tower-http = { version = "0.6", features = ["fs", "compression-gzip"] }
